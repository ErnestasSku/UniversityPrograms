1.1 Standartiniai duomenų tipai kaip int ir char yra pradedami rašyti mažają raide:
    * int a, int count, char word[]
    x int ABC, int Count, char WORD[]

1.2 Jeigu žodis yra sudurtinis, naudojamas camelCase:
    * int moveCount, int isPrime, char petName
    x int MoveCount, <..>

2. Programuotojo sukurti duomeų tipai pradedami rašyti didžiają raide (PascalCase)
    * Tree <..>, Node <..>, HashMap <..>
    x tree <..>, node <..>, ..

3. Funkcijos taip par rašomos PascalCase tipu
    * int GetAction(), char ReadFile()
    x int getAction(), char Readfile()

4. if/else if taisyklė:
    Jeigu prieš else if yra laužtiniai skliaustai jie rašomi prieš juos

    * if(<..>)
      {
        ...
      }else if()

    x if(<..>)
      {
          ...
      }
    else if

5. Laužtiniai skliaustai: 
    jie yra rašomi po while, for, if..
    * if()
      {

      }

    x if(){

      }

    taip pat, jeigu po if'o ar kitos funkcijos yra rašoma tik viena eilutė, kabliataškiai nebūtini

    * if()
    {
        single command
    }
    * if()
        single command
    
    
6. Pavadinimai turi būti prasmingi
    * int number, char name, int ammount
    x int a, b; char s1, s2
